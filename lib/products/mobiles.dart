import 'package:appp/products/mobiles_list.dart';
import 'package:flutter/material.dart';

class Mobiles extends StatelessWidget {
  const Mobiles({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ElectronicsPage(),
    );
  }
}
